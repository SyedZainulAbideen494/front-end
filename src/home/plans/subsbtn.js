import { Fragment } from "react";
import "./plans.css";

const Subsbtn = (props) => {
  return (
    <Fragment>
      <div className="subsbtn">
        <button>Subscribe</button>
      </div>
    </Fragment>
  );
};

export default Subsbtn;
